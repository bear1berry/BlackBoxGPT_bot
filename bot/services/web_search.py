from __future__ import annotations


async def research(query: str) -> str:
    """
    Заглушка под future web-research сервис.

    Сейчас веб-поиск реализует Perplexity API в профессиональном режиме,
    поэтому этот модуль не используется напрямую.

    В будущем сюда можно добавить:
      - отдельный вызов внешнего поискового API (SerpAPI, Tavily и т.п.)
      - обработку результатов и возврат short summary.
    """
    return (
        "Режим отдельного web-поиска пока не включён.\n\n"
        "Профессиональный режим уже умеет ходить в интернет через Perplexity."
    )
