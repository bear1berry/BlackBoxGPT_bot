from typing import Optional


def bold(text: str) -> str:
    return f"<b>{text}</b>"


def main_welcome(name: Optional[str] = None) -> str:
    greeting = f"👋 Привет, {bold(name)}!" if name else "👋 Привет!"
    return (
        f"{greeting}\n\n"
        f"Я — {bold('BlackBox GPT')}, твой универсальный ИИ-ассистент.\n\n"
        "Я умею: отвечать на вопросы, помогать с медициной, бизнесом, креативом, "
        "развитием личности и многим другим.\n\n"
        "Выбирай режим в нижнем меню или просто напиши свой первый запрос."
    )


def mode_menu_intro() -> str:
    return (
        "🧠 <b>Режимы</b>\n\n"
        "Выбери, в каком формате я буду работать:\n"
        "• 🧠 Универсальный — обычный умный ассистент обо всём.\n"
        "• 🩺 Медицина — помощь с медицинской информацией (без диагнозов).\n"
        "• 🔥 Наставник — дисциплина, цели, рост.\n"
        "• 💼 Бизнес — идеи, стратегии, сценарии.\n"
        "• 🎨 Креатив — тексты, визуальные идеи, посты.\n\n"
        "Нажми на нужный режим в меню ниже."
    )


def mode_updated(mode_human: str) -> str:
    return (
        f"✅ Режим обновлён: <b>{mode_human}</b>.\n\n"
        "Теперь просто напиши сообщение — и я буду отвечать в этом стиле."
    )


def profile_text(profile: dict | None) -> str:
    if not profile:
        return (
            "👤 <b>Профиль</b>\n\n"
            "Пока здесь пусто. Я запоминаю только то, что помогает лучше отвечать.\n"
            "Напиши, чем ты занимаешься и какие у тебя цели — и я сохраню это в профиль."
        )
    lines = ["👤 <b>Профиль</b>\n"]
    for key, value in profile.items():
        lines.append(f"• <b>{key}</b>: {value}")
    lines.append(
        "\nЧтобы обновить профиль — просто напиши новое описание, начиная со слова "
        "<code>Профиль:</code>."
    )
    return "\n".join(lines)


def subscription_text() -> str:
    return (
        "💎 <b>Подписка BlackBox GPT</b>\n\n"
        "Пока всё бесплатно и без ограничений.\n"
        "Позже здесь появятся тарифы и бонусы за рефералки. Следи за обновлениями 🔥"
    )


def referrals_text() -> str:
    return (
        "👥 <b>Рефералы</b>\n\n"
        "Реферальная система в разработке.\n"
        "Скоро ты сможешь приглашать друзей и получать бонусы за их активность."
    )


LLM_ERROR_FALLBACK = (
    "⚠️ <b>Временная ошибка модели</b>.\n\n"
    "Попробуй ещё раз чуть позже. Если ошибка повторяется, напиши разработчику бота."
)
