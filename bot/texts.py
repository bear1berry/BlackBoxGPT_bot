from html import escape as html_escape

MODES = {
    "universal": "🧠 Универсальный",
    "medicine": "🩺 Медицина",
    "mentor": "🔥 Наставник",
    "business": "💼 Бизнес",
    "creative": "🎨 Креатив",
}


def format_mode_title(mode: str) -> str:
    return MODES.get(mode, MODES["universal"])


def onboarding_text(full_name: str | None) -> str:
    safe_name = html_escape(full_name or "друг")
    return (
        f"👋 Привет, <b>{safe_name}</b>!\n\n"
        "<b>BlackBox GPT — Universal AI Assistant</b>.\n\n"
        "Выбери режим в нижнем меню и просто напиши свой запрос.\n"
        "Я адаптирую стиль и глубину ответа под выбранный режим."
    )


def main_menu_text() -> str:
    return (
        "⬇️ Внизу меню:\n"
        "🧠 Режимы · 👤 Профиль · 💎 Подписка · 👥 Рефералы\n\n"
        "Напиши сообщение или нажми «🧠 Режимы», чтобы выбрать формат общения."
    )


def mode_changed_text(mode: str) -> str:
    title = format_mode_title(mode)
    return (
        f"✅ Режим обновлён: <b>{title}</b>.\n\n"
        "Теперь все ответы будут адаптированы под этот режим.\n"
        "Просто напиши свой следующий запрос."
    )


def temporary_llm_error_text() -> str:
    return (
        "⚠️ <b>Временная ошибка модели</b>.\n\n"
        "Попробуй ещё раз через минуту.\n"
        "Если ошибка повторяется — напиши разработчику бота."
    )
