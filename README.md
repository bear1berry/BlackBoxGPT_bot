# BlackBox GPT — Universal AI Assistant (Telegram Bot)

Минималистичный, но мощный Telegram-бот с нижним таскбаром, режимами, подпиской через Crypto Bot,
реферальной системой и LLM (Perplexity / DeepSeek) с веб-доступом.

## Возможности

- 🧠 Режимы:
  - 🧠 Универсальный
  - 🩺 Медицина
  - 🔥 Наставник (использует `sonar-reasoning` для более глубоких ответов)
  - 💼 Бизнес
  - 🎨 Креатив
- 💎 Подписка Premium с оплатой через Crypto Bot (USDT или другой asset)
  - 1 месяц — 7.99$
  - 3 месяца — 25.99$
  - 12 месяцев — 89.99$
  - Полная автоматизация: от создания счёта до активации и продления Premium.
- 👥 Реферальная программа
  - У каждого пользователя своя ссылка вида: `https://t.me/BlackBoxGPT_bot?start=REF_CODE`
  - За каждого друга с оплаченной подпиской — +N дней Premium (см. `.env`).
- 👤 Профиль
  - Аватар пользователя (если доступен) + описание, статус подписки, текущий режим.
  - Внизу — кликабельная реферальная ссылка + кнопка.
- 🧷 Красивый онбординг при `/start`.
- 📡 LLM с веб-доступом (Perplexity), автоматическая подстройка под режим.
- 📚 Хранение истории диалога в базе (для контекста и «обучения» бота).
- ✨ Псевдо-стриминг ответов: текст набирается по блокам, как крутая статья.

---

## Стек

- Python 3.10+
- aiogram 3.x
- SQLAlchemy (async) + asyncpg (PostgreSQL)
- Perplexity API + DeepSeek API (по желанию)
- Crypto Bot (Crypto Pay API) для подписки

---

## Быстрый старт (локально)

1. Клонируем репозиторий:

```bash
git clone https://github.com/USERNAME/BlackBoxGPT_bot.git
cd BlackBoxGPT_bot
```

2. Создаём и активируем виртуальное окружение:

```bash
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

3. Настраиваем `.env`:

```bash
cp .env.example .env
nano .env
```

Обязательно заполни:

- `BOT_TOKEN` — токен от @BotFather
- `BOT_USERNAME` — юзернейм бота без `https://t.me/`
- `DATABASE_URL` — лучше PostgreSQL, например:  
  `postgresql+asyncpg://user:password@localhost:5432/blackboxgpt`
- `PERPLEXITY_API_KEY` — ключ Perplexity
- `LLM_PROVIDER` — `perplexity`, `deepseek` или `auto`
- `CRYPTOPAY_API_TOKEN` — токен от Crypto Bot (https://t.me/CryptoBot)

4. Запуск бота локально:

```bash
source venv/bin/activate
python -m bot.main
```

Если всё ок — в логах увидишь:

- `Starting BlackBox GPT bot...`
- `Start polling`
- `Run polling for bot @...`

---

## Деплой на сервер (systemd)

1. Скопируй код на сервер (git clone или rsync).

```bash
cd /home/USERNAME
git clone https://github.com/USERNAME/BlackBoxGPT_bot.git
cd BlackBoxGPT_bot

python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt

cp .env.example .env
nano .env  # заполни все значения
```

2. Скопируй unit-файл systemd:

```bash
sudo cp deploy/blackboxgpt.service /etc/systemd/system/blackboxgpt.service
sudo systemctl daemon-reload
sudo systemctl enable blackboxgpt
sudo systemctl start blackboxgpt
sudo systemctl status blackboxgpt
```

В `blackboxgpt.service` пути и пользователь должны совпадать с твоими:

```ini
User=bear1berry
WorkingDirectory=/home/bear1berry/BlackBoxGPT_bot
ExecStart=/home/bear1berry/BlackBoxGPT_bot/venv/bin/python -m bot.main
```

Если у тебя другой пользователь или путь — измени их в файле перед копированием.

3. Логи бота:

```bash
sudo journalctl -u blackboxgpt -f
```

---

## Как работает подписка через Crypto Bot

- При выборе плана (1/3/12 мес) бот:
  - создаёт инвойс через Crypto Pay API
  - сохраняет платёж в таблицу `payments`
  - отправляет пользователю ссылку на оплату и кнопку «✅ Проверить оплату»
- После оплаты пользователь нажимает «✅ Проверить оплату»:
  - бот проверяет статус инвойса через Crypto Pay
  - если `status == paid`:
    - продлевает/активирует Premium на нужное количество дней
    - помечает платёж как `paid`
- Если Premium истёк — при следующем обращении к базе пользователь автоматически
  переводится в `free`.

---

## Реферальная система

- У каждого пользователя ref-код = его `tg_id` в виде строки.
- Ссылка: `https://t.me/<BOT_USERNAME>?start=<REF_CODE>`.
- Когда новый пользователь впервые нажимает `/start` с payload:
  - он привязывается к пригласившему
  - пригласивший получает +`REFERRAL_REWARD_DAYS` Premium-дней
- Счётчик `referrals_count` хранится в таблице `users`.

---

## LLM и веб-доступ

- По умолчанию используется Perplexity (`LLM_PROVIDER=perplexity`).
- Модель выбирается в зависимости от режима:
  - Универсальный — `PERPLEXITY_MODEL_UNIVERSAL`
  - Наставник — `PERPLEXITY_MODEL_MENTOR` (по умолчанию `sonar-reasoning`)
  - Медицина — `PERPLEXITY_MODEL_MEDICINE`
  - Бизнес — `PERPLEXITY_MODEL_BUSINESS`
  - Креатив — `PERPLEXITY_MODEL_CREATIVE`
- Perplexity сам ходит в веб и поднимает актуальную информацию.
- История диалога сохраняется в `dialog_messages` и используется как контекст.

---

## Псевдо-стриминг ответов

Ответы от LLM не шлются одним куском — бот:

1. Разбивает текст на блоки.
2. Отправляет первый блок.
3. Постепенно дописывает остальные блоки через `edit_text`.

Выглядит как «умный», динамичный набор текста — но полностью совместим с HTML-разметкой.

---

## TODO / Идеи для расширения

- Хранение персональных длинных заметок пользователя (личная база знаний).
- Планировщик отчётов / напоминаний.
- Более гибкая градация подписок (Pro / VIP).
- Мульти-модальные модели (картинки, голос).

---

Если что-то падает:

1. Проверь `.env`.
2. Проверь `DATABASE_URL` (PostgreSQL или SQLite).
3. Посмотри логи:

```bash
sudo journalctl -u blackboxgpt -f
```

И уже с логами можно разбираться дальше.
