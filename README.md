# BlackBoxGPT – Universal AI Assistant (Telegram Bot)

Продакшн-скелет бота под твой Roadmap v1: единое LLM-ядро, режимы, подписки, рефералка, профиль,
pseudo-стриминг ответов, минималистичный UX без инлайн-клавиатур (кроме будущей кнопки «Продолжить»).

## Архитектура проекта

```text
BlackBoxGPTBot/
├── bot/
│   ├── main.py               # Точка входа, запуск aiogram
│   ├── config.py             # Settings на pydantic-settings
│   ├── __init__.py
│   ├── db/
│   │   ├── __init__.py
│   │   └── db.py             # asyncpg-пул + хелперы
│   ├── keyboards/
│   │   ├── __init__.py
│   │   ├── common.py
│   │   └── main_menu.py      # таскбар-меню + подменю
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── start.py          # /start + онбординг
│   │   ├── menu.py           # Режимы, навигация, Назад
│   │   ├── profile.py        # 👤 Профиль
│   │   ├── subscription.py   # 💎 Подписка (Crypto Pay)
│   │   ├── referrals.py      # 👥 Рефералы
│   │   └── chat.py           # Основной диалог + стриминг
│   └── services/
│       ├── __init__.py
│       ├── llm.py            # LLMClient (DeepSeek + Perplexity)
│       ├── text_postprocess.py  # Очистка и форматирование текста
│       ├── storage.py        # users, referrals, режимы
│       ├── analytics.py      # usage_stats + подсчёт токенов
│       ├── payments_crypto.py # Crypto Bot / Crypto Pay
│       └── web_search.py     # Заглушка под research-режим
├── migrations/
│   └── 001_init.sql          # users, subscriptions, referrals, payments, usage_stats, dialogs
├── deploy/
│   ├── aimedbot.service      # systemd unit
│   └── yandex_cloud_deploy.md
├── .env.example
├── requirements.txt
└── README.md
```

## Что уже реализовано по Roadmap v1

**Level 0–1** (ядро, архитектура, стабильность):
- Чёткая структура проекта (`bot/main.py`, `config.py`, `routers/`, `services/`, `db/`, `keyboards/`).
- LLM-ядро: `LLMClient` с `ask()` и `ask_stream()`; адаптер под DeepSeek + fallback на Perplexity.
- Централизованная предобработка ответа: `text_postprocess.prepare_answer()` — очистка артефактов, заголовки, списки.
- База данных PostgreSQL + миграции SQL (users, subscriptions, referrals, payments, usage_stats, dialogs).
- Деплой через systemd + понятный `ExecStart` + инструкция для Yandex Cloud.

**Level 2** (подписки, рефералка, мини-кабинет):
- Таблицы `subscriptions`, `payments`, `referrals`.
- Раздел «💎 Подписка» с тарифами 1/3/12 месяцев и генерацией инвойса через Crypto Pay (если задан токен).
- Сохранение платежа в таблицу `payments` со статусом `pending` (вебхук можно добавить отдельно).
- Профиль «👤 Профиль»: тариф, дата окончания, счётчик запросов за сегодня, оценка токенов.
- Раздел «👥 Рефералы»: реф-код, реф-ссылка, количество приглашённых и премиум-приглашённых.

**Level 3** (UX/UI и стиль):
- Таскбар через `ReplyKeyboardMarkup`: 🧠 Режимы / 👤 Профиль / 💎 Подписка / 👥 Рефералы.
- Внутренние меню с кнопкой «⬅️ Назад».
- Единый тон сообщений: коротко, структурировано, с эмодзи, без визуальной каши.
- Инлайн-кнопки не используются (оставлен задел только под будущую кнопку «Продолжить» для длинных ответов).

**Level 4** (интеллект и персонализация):
- Режимы: Универсальный, Профессиональный, Наставник, Медицина — переключаются и сохраняются в БД.
- Простейшая адаптация стиля под пользователя: `infer_style_from_text()` анализирует сленг, эмодзи, знаки
  восклицания и регулирует формальность/эмоциональность.
- В медицине в system prompt добавлены ограничения: только общие рекомендации, без диагнозов и дозировок.

**Level 5** (масштаб и производительность — базовый задел):
- Разделение на сервисы: LLM, аналитика, платежи, web-search (заглушка).
- Pseudo-стриминг: ответ модели режется на чанки и печатается «вживую» через `edit_text`.

## Быстрый старт локально

1. Установи зависимости:

```bash
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

2. Создай PostgreSQL-базу и пропиши DSN в `.env` (см. `.env.example`).

3. Прогони миграции:

```bash
export DB_DSN="postgresql://user:password@localhost:5432/blackboxgpt"
psql "$DB_DSN" -f migrations/001_init.sql
```

4. Запусти бота:

```bash
source venv/bin/activate
python -m bot.main
```

## Ключевые моменты с учётом прошлых ошибок

- **aiogram 3.13+**: `parse_mode` больше не передаётся в конструктор `Bot` напрямую — используется
  `DefaultBotProperties(parse_mode=ParseMode.MARKDOWN)` в `bot/main.py` (исправляет прошлую ошибку).
- **config.Settings**: класс `Settings` лежит в `bot/config.py`, и из него создаётся экземпляр `settings` —
  импорт `from bot.config import settings` работает корректно и не падает с `ImportError`.
- **LLM-стриминг**: `ask_stream()` не принимает лишних аргументов (типа `user_prompt` с неправильным именем) —
  сигнатура единая и используется во всех местах согласованно.
- **systemd ExecStart**: в `deploy/aimedbot.service` прописан полный путь до Python в venv и рабочей директории —
  достаточно заменить `your-linux-user` и путь до проекта под свою ВМ.
- **Миграции**: используются обычные SQL-файлы, прогоняются одной командой `psql "$DB_DSN" -f migrations/001_init.sql`
  без отдельного "PLANNER_DATABASE_URL" и прочих артефактов.

## Дальнейшее развитие (TODO по Roadmap)

- Добавить реальный вебхуки от Crypto Pay и изменение статуса подписки (is_premium, subscription_expires_at).
- Реализовать хранение и усечённую историю диалога в таблице `dialogs` с умным контекст-окном.
- Вынести web-research в отдельный сервис и подключить его в профессиональном режиме.
- Добавить продвинутую орфопроверку (через внешний сервис) в `text_postprocess.py`.
- Развернуть отдельные микросервисы для LLM, платежей и аналитики при росте нагрузки.
