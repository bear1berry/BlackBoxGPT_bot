# BlackBox GPT — Universal AI Assistant

Telegram-бот **BlackBox GPT** — это универсальный ИИ‑ассистент с режимами (универсальный, медицина,
наставник, бизнес, креатив), адаптивным выбором моделей (DeepSeek / Perplexity) и голосовыми
сообщениями через Yandex Cloud (STT/TTS).

---

## Возможности

- 🧠 **Режимы работы**
  - Универсальный ассистент
  - Медицина (без постановки диагнозов и назначения лечения)
  - Наставник (дисциплина, рост, мотивация)
  - Бизнес (идеи, стратегии, продукты)
  - Креатив (тексты, идеи, сценарии)

- 👤 **Профиль пользователя**
  - Явное описание профиля и целей
  - Память о режиме и базовой информации о пользователе (в JSON-файле)

- 💬 **LLM ядро**
  - DeepSeek: быстрые универсальные ответы
  - Perplexity: запросы с упором на поиск и контекст из интернета
  - Простое адаптивное правило выбора модели

- 🎙 **Голос**
  - Распознавание речи (STT) через Yandex Cloud
  - Озвучка ответа (TTS) через Yandex Cloud
  - Пользователь может общаться голосом, получать текст и голосовой ответ

- 🧩 **Интерфейс**
  - Только нижнее **Reply‑меню**, без инлайн‑кнопок
  - Структурированное меню: Режимы, Профиль, Подписка, Рефералы

---

## Структура проекта

```text
BlackBoxGPT_bot/
├─ bot/
│  ├─ __init__.py
│  ├─ main.py              # Точка входа бота
│  ├─ config.py            # Конфигурация (pydantic-settings)
│  ├─ texts.py             # Текстовые шаблоны и константы
│  ├─ keyboards/
│  │  ├─ __init__.py
│  │  └─ main_menu.py      # Главное меню и подменю
│  ├─ routers/
│  │  ├─ __init__.py
│  │  ├─ start.py          # /start + онбординг
│  │  ├─ navigation.py     # Режимы, профиль, подписка, рефералы
│  │  ├─ chat.py           # Основной диалог
│  │  └─ voice.py          # Голосовой чат (Yandex STT/TTS)
│  └─ services/
│     ├─ __init__.py
│     ├─ storage.py        # Простое JSON‑хранилище профилей/режимов
│     ├─ llm.py            # Логика DeepSeek / Perplexity
│     └─ yandex_voice.py   # Работа с Yandex Cloud STT/TTS
├─ .env.example            # Пример файла окружения
├─ requirements.txt
└─ README.md
```

---

## 1. Подготовка окружения (локальный запуск)

### 1.1. Клонирование репозитория

```bash
git clone https://github.com/YOUR_USERNAME/BlackBoxGPT_bot.git
cd BlackBoxGPT_bot
```

### 1.2. Виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

### 1.3. Файл `.env`

1. Скопируй пример:
   ```bash
   cp .env.example .env
   ```
2. Заполни `.env` своими данными:

   - `bot_token` — токен Telegram‑бота от BotFather
   - `deepseek_api_key` — ключ DeepSeek
   - `perplexity_api_key` — ключ Perplexity (если нужен)
   - `yandex_iam_token` и `yandex_folder_id` — данные из Yandex Cloud
   - Опционально: `database_url` (на будущее), логирование и таймауты

### 1.4. Тестовый запуск

```bash
source venv/bin/activate
python -m bot.main
```

Если всё ок, в консоли появится лог старта, а бот начнёт отвечать в Telegram.

---

## 2. Деплой на Yandex Cloud (VM + systemd)

Ниже пример для Ubuntu 22.04+ на виртуальной машине в Yandex Cloud.

### 2.1. Подготовка сервера

```bash
sudo apt update
sudo apt install -y python3 python3-venv git
```

### 2.2. Клонирование и установка зависимостей

```bash
cd ~
git clone https://github.com/YOUR_USERNAME/BlackBoxGPT_bot.git
cd BlackBoxGPT_bot

python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

Скопируй и заполни `.env` на сервере так же, как локально.

### 2.3. systemd‑unit для автозапуска

Создай файл `/etc/systemd/system/blackboxgpt.service`:

```ini
[Unit]
Description=BlackBox GPT Telegram Bot
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=YOUR_LINUX_USER
WorkingDirectory=/home/YOUR_LINUX_USER/BlackBoxGPT_bot
Environment="PYTHONPATH=/home/YOUR_LINUX_USER/BlackBoxGPT_bot"
ExecStart=/home/YOUR_LINUX_USER/BlackBoxGPT_bot/venv/bin/python -m bot.main
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

Обнови `YOUR_LINUX_USER` и путь, если нужно.

Затем выполни:

```bash
sudo systemctl daemon-reload
sudo systemctl enable blackboxgpt
sudo systemctl start blackboxgpt
sudo systemctl status blackboxgpt
```

Для просмотра логов:

```bash
sudo journalctl -u blackboxgpt -f
```

---

## 3. Как работает меню

- Главный экран:
  - 🧠 Режимы
  - 👤 Профиль
  - 💎 Подписка
  - 👥 Рефералы

- Режимы:
  - Выбор режима (универсальный, медицина, наставник, бизнес, креатив)
  - Сохранение режима в JSON‑хранилище для каждого пользователя

- Профиль:
  - Просмотр текущего профиля
  - Обновление через сообщение вида:  
    `Профиль: я врач-эпидемиолог, люблю минимализм...`

- Голос:
  - Отправляешь голосовое → Yandex STT → LLM → Yandex TTS → получаешь голосовой ответ

---

## 4. Замечания по продакшену

- JSON‑хранилище (`bot/data/users.json`) подходит для MVP и первых запусков.
  Для серьёзной нагрузки лучше перевести хранение профилей/подписок в PostgreSQL
  (в проект уже добавлены зависимости SQLAlchemy/asyncpg).
- Для DeepSeek и Perplexity обязательно выставить корректные тарифы и лимиты.
- Для Yandex Cloud:
  - следи за расходами на STT/TTS,
  - не храни секретные ключи в репозитории, только в `.env` или в секретах VM.

---

## 5. Быстрый чеклист запуска

1. Создай бота у BotFather, получи `bot_token`.
2. Получи ключи DeepSeek / Perplexity / Yandex Cloud.
3. Склонируй репозиторий и установи зависимости.
4. Настрой `.env`.
5. Запусти `python -m bot.main` локально и проверь работу.
6. Задеплой на Yandex Cloud через `systemd`.
7. Следи за логами и оптимизируй промпт/режимы под свою аудиторию.

---

Готово. Это базовая, но уже боевая версия **BlackBox GPT** с меню, режимами,
LLM‑ядром DeepSeek/Perplexity и голосом через Yandex Cloud.
