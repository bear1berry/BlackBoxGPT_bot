# BlackBox GPT — Universal AI Assistant (Telegram Bot)

BlackBox GPT — это универсальный ИИ-ассистент в Telegram с режимами, профилем,
лимитами, реферальной системой и премиальным UX. Основа — Roadmap со стальным
фундаментом, режимами, профилем и лимитами (Уровень 1–2).

Бот построен на:

- Python 3.10+
- [aiogram 3](https://docs.aiogram.dev)
- DeepSeek Chat API
- SQLAlchemy 2 (async) + SQLite / PostgreSQL
- systemd для продакшн-деплоя (Yandex Cloud / любой Linux-сервер)

---

## Возможности

**Режимы** (управляются кнопкой «🧠 Режимы»):

- Универсальный — умный ассистент “про всё”
- Медицина — аккуратный справочный режим (без назначения лечения)
- Наставник — личностный рост, дисциплина, режим дня
- Бизнес — идеи, стратегии, анализ
- Креатив — тексты, концепты, идеи дизайна

**Профиль** («👤 Профиль»):

- Telegram ID и username
- Текущий тариф (Free / Pro / VIP)
- Текущий режим
- Сколько сообщений отправлено сегодня
- Количество рефералов
- Твоя персональная реферальная ссылка

**Подписка и лимиты** («💎 Подписка»):

- Free — 30 сообщений в день
- Pro — 200 сообщений в день
- VIP — 1000 сообщений в день

Логика лимитов реализована в БД. Сейчас смена тарифа делается через админ-команду
`/set_tier` (интеграция с CryptoBot/Crypto Pay может быть добавлена поверх этого ядра).

**Рефералка** («👥 Рефералы»):

- Для каждого пользователя генерируется `ref_<telegram_id>`
- Реферальная ссылка вида `https://t.me/<bot_username>?start=ref_<telegram_id>`
- При старте по реф-ссылке у реферера увеличивается счётчик рефералов

---

## Архитектура проекта

```text
BlackBoxGPT_bot/
  bot/
    __init__.py
    main.py
    config.py
    texts.py
    keyboards.py
    routers/
      __init__.py
      start.py
      navigation.py
      chat.py
      admin.py
  services/
    __init__.py
    llm.py
  db/
    __init__.py
  deploy/
    blackboxgpt.service
  requirements.txt
  .env.example
  README.md
```

- `bot/main.py` — входная точка, запуск aiogram Dispatcher
- `bot/config.py` — настройки через `pydantic-settings` (`.env`)
- `bot/routers/` — все хэндлеры:
    - `start.py` — `/start` + онбординг + реф-коды
    - `navigation.py` — меню, режимы, профиль, подписка, рефералы
    - `chat.py` — основной диалог с LLM
    - `admin.py` — админ-команда `/set_tier`
- `bot/keyboards.py` — нижний таскбар и клавиатуры режимов
- `bot/texts.py` — тексты сообщений (приветствие, профиль, подписка и т.п.)
- `services/llm.py` — единый вход в LLM (DeepSeek)
- `db/__init__.py` — модели SQLAlchemy, инициализация БД и вспомогательные функции
- `deploy/blackboxgpt.service` — юнит-файл systemd для продакшн-деплоя

Архитектура соответствует первому уровню Roadmap: единое ядро LLM, чёткая структура
проекта, БД под контролем и понятный деплой.

---

## Быстрый старт (локально)

### 1. Клонировать репозиторий

```bash
git clone https://github.com/<your-username>/BlackBoxGPT_bot.git
cd BlackBoxGPT_bot
```

### 2. Создать виртуальное окружение и установить зависимости

```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

pip install --upgrade pip
pip install -r requirements.txt
```

### 3. Настроить `.env`

Скопируй пример и заполни:

```bash
cp .env.example .env
```

В `.env` нужно указать:

- `BOT_TOKEN` — токен бота из BotFather
- `BOT_USERNAME` — username бота без `@` (например, `BlackBoxGPT_bot`)
- `DEEPSEEK_API_KEY` — ключ к DeepSeek API
- `ADMIN_IDS` — список Telegram ID админов через запятую
- `DB_URL` — URL БД (по умолчанию SQLite в `./data/bot.db`)

Примеры:

```env
BOT_TOKEN=1234567890:ABCDEF...
BOT_USERNAME=BlackBoxGPT_bot
DEEPSEEK_API_KEY=sk-...

ADMIN_IDS=123456789,987654321
DB_URL=sqlite+aiosqlite:///./data/bot.db
```

Для PostgreSQL:

```env
DB_URL=postgresql+asyncpg://user:password@host:5432/dbname
```

### 4. Запуск локально

```bash
source venv/bin/activate
python -m bot.main
```

В логах ты увидишь что-то вроде:

```text
2025-..-.. ..:..:.. [INFO] root: Starting polling as @BlackBoxGPT_bot
```

Теперь можно писать боту в Telegram.

---

## Что увидит пользователь

1. Пишет `/start`
2. Получает приветствие и нижний таскбар:
    - 🧠 Режимы
    - 👤 Профиль
    - 💎 Подписка
    - 👥 Рефералы
3. Выбирает режим в «🧠 Режимы» (универсальный, медицина, наставник, бизнес, креатив)
4. Пишет любой вопрос — бот отвечает через DeepSeek с учётом выбранного режима
5. В «👤 Профиль» видит:
    - тариф,
    - режим,
    - лимит сообщений на сегодня,
    - рефералов,
    - свою реф-ссылку
6. В «💎 Подписка» видит текущие лимиты и пояснение
7. В «👥 Рефералы» видит счётчик и ссылку для приглашения друзей

Если дневной лимит исчерпан, бот отправит аккуратное сообщение и предложит
апгрейд тарифа.

---

## Админ-функции

### /set_tier

Команда доступна только пользователям, чьи Telegram ID указаны в `ADMIN_IDS`.

Формат:

```text
/set_tier <telegram_id> <tier>
```

Где `tier` — один из:

- `free`
- `pro`
- `vip`

Пример:

```text
/set_tier 123456789 vip
```

После этого у пользователя `123456789` лимит будет как у VIP-тарифа.

---

## Деплой на Yandex Cloud (или любой Linux-сервер)

1. Поднять VM (Ubuntu 22.04+ / Debian)
2. Установить системные зависимости и Python

   ```bash
   sudo apt update
   sudo apt install -y python3 python3-venv python3-pip git
   ```

3. Клонировать репозиторий

   ```bash
   cd /home/ubuntu
   git clone https://github.com/<your-username>/BlackBoxGPT_bot.git
   cd BlackBoxGPT_bot
   ```

4. Создать виртуальное окружение и установить зависимости

   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install --upgrade pip
   pip install -r requirements.txt
   ```

5. Создать `.env` на сервере

   ```bash
   cp .env.example .env
   nano .env
   ```

   Заполни токены и ключи.

6. Проверить запуск вручную

   ```bash
   source venv/bin/activate
   python -m bot.main
   ```

   Убедись, что бот стартует и отвечает. Останови его `Ctrl+C`.

7. Настроить systemd

   ```bash
   sudo cp deploy/blackboxgpt.service /etc/systemd/system/blackboxgpt.service
   sudo systemctl daemon-reload
   sudo systemctl enable blackboxgpt
   sudo systemctl start blackboxgpt
   sudo systemctl status blackboxgpt
   ```

   Если нужно обновить код:

   ```bash
   cd /home/ubuntu/BlackBoxGPT_bot
   git pull
   source venv/bin/activate
   pip install -r requirements.txt
   sudo systemctl restart blackboxgpt
   ```

8. Логи

   ```bash
   sudo journalctl -u blackboxgpt -f
   ```

---

## Производительность и стабильность

- Все запросы к LLM проходят через единый модуль `services/llm.py`
- БД инициализируется один раз при старте (`init_engine + create_all`)
- Лимиты считаются на уровне БД (поле `daily_message_count` и `last_message_date`)
- Логирование настроено через `logging.basicConfig` в `bot/main.py`

Для дальнейшего масштабирования можно:

- Перейти с SQLite на PostgreSQL (см. `DB_URL`)
- Вынести LLM-запросы в отдельный сервис или очередь
- Добавить метрики и health-check эндпоинт (если позже будет web-обвязка)

---

## Что уже покрыто из Roadmap

- **Уровень 1. Стальной фундамент**:
    - Чёткая структура проекта
    - Единое LLM-ядро (DeepSeek)
    - БД + миграции через SQLAlchemy (модели + auto-create)
    - Продакшн-деплой через systemd (Yandex Cloud-ready)
- **Уровень 2. Продукт**:
    - Режимы (универсальный, медицина, наставник, бизнес, креатив)
    - Профиль (тариф, режим, лимиты, рефералы и реф-ссылка)
    - Лимиты и базовая тарификация (Free / Pro / VIP)
- **Уровень 3. Деньги и рост**:
    - Заложены сущности тарификации и рефералок
    - Админ-команда `/set_tier` как ручное управление подписками
    - UI для разделов «Подписка» и «Рефералы»

Следующие шаги по Roadmap — добавление реальных платежей через CryptoBot,
аналитики, персонализации стиля и web-доступа.

---

## Лицензия

Используй, модифицируй, развивай под свои задачи. Если будешь публично ссылаться,
можно указать название: **BlackBox GPT — Universal AI Assistant**.
